import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from '@/components/providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Career Advisor - Digital Guidance Platform for Students',
  description: 'A comprehensive platform to guide students in choosing the right career path, degree courses, and government colleges.',
  keywords: ['career guidance', 'education', 'students', 'degree courses', 'government colleges'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  )
}
